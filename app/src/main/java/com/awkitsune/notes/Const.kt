package com.awkitsune.notes

class Const {
    companion object {
        const val SETTINGS_SHARED_PREFERENCES = "AppSettings"

        const val NOTES_SHARED_PREFERENCES = "Notes"
        const val NOTES_NAME = "Notes"
    }
}